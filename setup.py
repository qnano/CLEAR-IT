from setuptools import setup, find_packages

setup(
    name='clearit',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # your dependencies here
    ],
)

setup(
    name='maps',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # your dependencies here
    ],
)
